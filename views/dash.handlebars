    <div class="">
        <a href="/dash/create/"><button id="create-post" class="">create post</button></a>
        
        <h2 class=""> Your posts and comments</h2><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dash/edit/{{post.id}}"><button class="">edit post</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="">Shame on you! You have not posted anything yet</h6>

{{/if}}
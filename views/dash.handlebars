    <div class="">        
        <h2 class=""> Your posts</h2><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dash/edit/{{post.id}}"><button class="edit-post">Edit</button></a>
        </div>
    {{/each}}

{{else}}
<h6 class="">Shame on you! You have not posted anything yet</h6>

{{/if}}
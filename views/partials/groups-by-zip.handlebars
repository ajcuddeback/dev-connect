<div class="group-info">
    <p>{{group_title}}</p>
    <p>{{group_text}}</p>
    <p>Zip Code: {{group_zip}}</p>
</div>
<button class="join-group grey-red-btn" data-id="{{id}}">Join {{group_title}}</button>
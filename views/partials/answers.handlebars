<div class="answers">
  {{#each this}}
  <section class="answers-item row">
    <div class="meta">
      {{user.username}} {{format_date created_at}} || 
    </div>
    <div class="answer-text">
      {{answer_text}}
    </div>
  </section>
  {{/each}}
</div>
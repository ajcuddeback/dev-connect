{{> question-info question}}

{{#if loggedIn}}
<form class="answer-form">
  <div>
    <textarea name="answer-body"></textarea>
  </div>

  <div>
    <button type="submit" class="grey-red-btn">Answer</button>
  </div>
</form>
{{/if}}

{{> answers question.answers}}

{{#if loggedIn}}
<script src="/javascript/answer.js"></script>
{{/if}}